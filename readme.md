# Чистая архитектура в Node.js
## Практическая реализация без фреймворков

![Node.js](https://img.shields.io/badge/Node.js-24%2B-green) ![TypeScript](https://img.shields.io/badge/TypeScript-5%2B-blue) ![License](https://img.shields.io/badge/License-MIT-yellow)

> Исходники примеров к докладу для конференции DUMP (Санкт-Петербург)  
> 📅 **Дата выступления:** 13 февраля 2026  
> 🔗 **Лендинг:** [Dump SPB](https://dump-spb.ru/chistaya-arhitektura-v-nodejs)  
> 🍿 **Запись доклада**: n/a  
> 📚 **Презентация**: n/a  

---

## Оглавление

- [Чистая архитектура в Node.js](#чистая-архитектура-в-nodejs)
  - [Практическая реализация без фреймворков](#практическая-реализация-без-фреймворков)
  - [Оглавление](#оглавление)
  - [📖 О проекте](#-о-проекте)
  - [🛠 Стек технологий](#-стек-технологий)
  - [🚀 Быстрый старт](#-быстрый-старт)
  - [📜 Скрипты](#-скрипты)
  - [🌐 API](#-api)
    - [POST /register — регистрация пользователя](#post-register--регистрация-пользователя)
    - [POST /login — вход](#post-login--вход)
  - [🏗 Архитектура и слои](#-архитектура-и-слои)
  - [📁 Структура проекта](#-структура-проекта)
  - [🧩 Паттерны проектирования](#-паттерны-проектирования)
  - [🧪 Тесты](#-тесты)
  - [📚 Документация](#-документация)

---

## 📖 О проекте

Демонстрационное приложение, построенное по принципам **Clean Architecture** (Роберт Мартин). Реализует два эндпоинта — регистрацию пользователя и вход — поверх собственного мини-HTTP-фреймворка, без Express или Fastify.

Цель проекта — **не** заменить существующие фреймворки, а показать на практике:

- как организовать код так, чтобы бизнес-логика не зависела от фреймворка, БД и HTTP;
- как направить зависимости «внутрь»;
- как IoC-контейнер связывает абстракции с реализациями в единственной точке сборки;
- как архитектура делает тестирование естественным, а не болезненным.

Проект намеренно прост по функциональности, чтобы фокус оставался на **архитектурных решениях и паттернах**, а не на бизнес-логике.

---

## 🛠 Стек технологий

| Технология | Назначение |
|---|---|
| **Node.js 24+** | Рантайм (встроенный тест-раннер, экспериментальный SQLite API) |
| **TypeScript** | Статическая типизация, ESM-модули |
| **Inversify 7** | IoC-контейнер, Dependency Injection через декораторы |
| **class-validator** | Декларативная валидация DTO |
| **class-transformer** | Преобразование plain-объектов в экземпляры классов |
| **node:sqlite** | Встроенная in-memory БД (флаг `--experimental-sqlite`) |
| **node:crypto** | Хеширование паролей через scrypt |
| **node:test** | Встроенный тест-раннер |
| **tsx** | Запуск TypeScript без предварительной компиляции |

---

## 🚀 Быстрый старт

```bash
# 1. Клонировать репозиторий
git clone [<url>](https://github.com/AntonovIgor/dump-2026-clean-architectire-in-nodejs)
cd clean-architecture-demo

# 2. Установить зависимости
npm install

# 3. Запустить сервер (порт 3000)
npm run dev

# 4. Проверить
curl -s -X POST http://localhost:3000/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","firstName":"John","lastName":"Doe","dateOfBirth":"1990-01-15","password":"secret123"}'
```

---

## 📜 Скрипты

| Команда | Что делает |
|---|---|
| `npm run dev` | Запуск dev-сервера через tsx (порт 3000) |
| `npm start` | Запуск через node с --import tsx |
| `npm test` | Запуск всех тестов (unit + integration + e2e) |

---

## 🌐 API

### POST /register — регистрация пользователя

**Запрос:**
```json
{
  "email": "john@example.com",
  "firstName": "John",
  "lastName": "Doe",
  "dateOfBirth": "1990-01-15",
  "password": "secret123"
}
```

**Ответы:**

| Код | Описание |
|-----|----------|
| `201` | Успешная регистрация. Возвращает пользователя (без пароля) |
| `409` | Email уже занят |
| `422` | Ошибки валидации |

### POST /login — вход

**Запрос:**
```json
{
  "email": "john@example.com",
  "password": "secret123"
}
```

**Ответы:**

| Код | Описание |
|-----|----------|
| `200` | Успешный вход. Возвращает массив всех пользователей (без паролей) |
| `401` | Неверные учётные данные |
| `422` | Ошибки валидации |

Полные примеры cURL-запросов — в файле [queries.md](queries.md).

---

## 🏗 Архитектура и слои

Зависимости в коде направлены **только внутрь** — от внешних слоёв к внутренним:

```
┌──────────────────────────────────────────────────────────┐
│                     PRESENTATION                         │
│              (контроллеры, фильтры)                      │
│  ┌────────────────────────────────────────────────────┐  │
│  │                 APPLICATION                        │  │
│  │        (use cases, запросы/ответы)                 │  │
│  │  ┌──────────────────────────────────────────────┐  │  │
│  │  │                 DOMAIN                       │  │  │
│  │  │        (сущности, интерфейсы,                │  │  │
│  │  │         доменные сервисы)                    │  │  │
│  │  └──────────────────────────────────────────────┘  │  │
│  └────────────────────────────────────────────────────┘  │
│                                                          │
│                    INFRASTRUCTURE                         │
│           (БД, хеширование, IoC-контейнер)               │
└──────────────────────────────────────────────────────────┘
```

| Слой | Ответственность | Зависит от |
|------|-----------------|------------|
| **Domain** | Сущности, бизнес-правила, интерфейсы репозиториев | Ничего |
| **Application** | Use cases, контракты запросов/ответов | Domain |
| **Infrastructure** | SQLite, scrypt, валидатор, IoC-контейнер | Domain, Application |
| **Presentation** | HTTP-контроллеры, DTO, exception filters | Application |
| **Framework** | Мини-HTTP-сервер, middleware, router | Ничего (утилита) |

---

## 📁 Структура проекта

```
src/
├── main.ts                                       Composition Root — точка сборки
├── container.ts                                  Конфигурация Inversify-контейнера
├── shared/
│   └── types.ts                                  Symbol-токены для DI
│
├── domain/                                       ДОМЕН — ядро, 0 внешних зависимостей
│   ├── entity/
│   │   └── user.entity.ts                        Сущность User (id, email, имя, пароль)
│   ├── repository/
│   │   └── user-repository.interface.ts          Интерфейс IUserRepository
│   ├── exception/
│   │   ├── invalid-credentials.exception.ts      Неверные учётные данные
│   │   └── user-already-exists.exception.ts      Пользователь уже существует
│   └── service/
│       ├── password-hasher.interface.ts           Интерфейс IPasswordHasher
│       └── user.service.ts                       Доменный сервис (регистрация, логин)
│
├── application/                                  USE CASES — сценарии использования
│   ├── exception/
│   │   └── validation.exception.ts               Ошибки валидации (422)
│   ├── request/
│   │   ├── register-user.request.ts              Контракт запроса регистрации
│   │   └── login-user.request.ts                 Контракт запроса логина
│   ├── response/
│   │   └── user.response.ts                      Контракт ответа (без пароля)
│   └── validation/
│       └── validator.interface.ts                Интерфейс валидатора
│   └── use-case/
│       ├── register-user.use-case.ts             Сценарий регистрации
│       └── login-user.use-case.ts                Сценарий логина
│
├── infrastructure/                               ИНФРАСТРУКТУРА — технические реализации
│   ├── database/
│   │   └── sqlite.database.ts                    Обёртка над DatabaseSync (:memory:)
│   ├── repository/
│   │   └── sqlite-user.repository.ts             Реализация IUserRepository (SQLite)
│   ├── security/
│   │   └── scrypt-password-hasher.ts             Реализация IPasswordHasher (scrypt)
│   └── validation/
│       └── class-validator.validator.ts          Адаптер class-validator
│
├── presentation/                                 ПРЕДСТАВЛЕНИЕ — адаптация HTTP
│   ├── controller/
│   │   └── user.controller.ts                    Контроллер (POST /register, POST /login)
│   ├── dto/
│   │   ├── register-user.dto.ts                  DTO регистрации + валидация
│   │   ├── login-user.dto.ts                     DTO логина + валидация
│   │   └── user-response.dto.ts                  DTO ответа (без пароля)
│   └── exception-filter/
│       ├── http-exception.filter.ts              Обработка HttpException → статус-код
│       └── validation-exception.filter.ts        Обработка ValidationException → 422
│       ├── user-already-exists-exception.filter.ts Обработка 409
│       └── invalid-credentials-exception.filter.ts Обработка 401
│
└── framework/                                    МИНИ-ФРЕЙМВОРК — HTTP-инфраструктура
    ├── http-exception.ts                         Класс HttpException (statusCode + message)
    ├── request.ts                                HttpRequest — обёртка над IncomingMessage
    ├── response.ts                               HttpResponse — обёртка над ServerResponse
    ├── middleware.ts                              IMiddleware + runMiddlewarePipeline()
    ├── json-body-parser.middleware.ts             Middleware для парсинга JSON-тела
    ├── controller.abstract.ts                    Абстрактный контроллер (bindRoutes)
    ├── exception-filter.abstract.ts              Абстрактный фильтр (canHandle + catch)
    ├── router.ts                                 Маршрутизатор с поддержкой :params
    └── server.ts                                 HttpServer — фасад над http + router + middleware

tests/
├── unit/
│   ├── domain/
│   │   └── user.service.test.ts                  UserService с мок-репозиторием и мок-хешером
│   ├── application/
│   │   ├── register-user.use-case.test.ts        Валидация, маппинг, обработка ошибок
│   │   └── login-user.use-case.test.ts           Валидация, credentials, маппинг
│   └── infrastructure/
│       └── scrypt-password-hasher.test.ts        Формат хеша, соль, сравнение
├── integration/
│   └── user.repository.test.ts                   SqliteUserRepository + реальная SQLite
└── e2e/
    └── api.test.ts                               Полный стек: HTTP → БД → HTTP
```

---

## 🧩 Паттерны проектирования

Проект демонстрирует 12 паттернов в рамках одного приложения:

| Паттерн | Где в проекте | Суть |
|---------|---------------|------|
| **Repository** | `IUserRepository` → `SqliteUserRepository` | Абстракция доступа к данным |
| **Dependency Inversion** | Домен определяет интерфейсы, инфраструктура реализует | Зависимости направлены внутрь |
| **IoC / DI** | Inversify-контейнер + `@inject()` | Зависимости предоставляются извне |
| **Use Case (Interactor)** | `RegisterUserUseCase`, `LoginUserUseCase` | Одно действие = один класс с `execute()` |
| **DTO** | `RegisterUserDto`, `UserResponseDto` | Валидация и представление |
| **Strategy** | `IPasswordHasher` → `ScryptPasswordHasher` | Заменяемый алгоритм хеширования |
| **Chain of Responsibility** | `runMiddlewarePipeline()` | Цепочка middleware |
| **Template Method** | `AbstractController`, `AbstractExceptionFilter` | Базовый класс задаёт каркас |
| **Factory Method** | `UserResponseDto.fromResponse()` | Создание DTO из ответа |
| **Adapter** | `HttpRequest`/`HttpResponse`, контроллер | Адаптация HTTP к use cases |
| **Composition Root** | `main.ts` | Единственная точка сборки графа |
| **Facade** | `HttpServer` | Упрощённый интерфейс над подсистемами |

---

## 🧪 Тесты

```bash
npm test
```

Тесты на трёх уровнях:

| Уровень | Файлы | Что проверяет |
|---------|-------|---------------|
| **Unit** | `user.service.test.ts`, `register-user.use-case.test.ts`, `login-user.use-case.test.ts`, `scrypt-password-hasher.test.ts` | Бизнес-логика и оркестрация с моками |
| **Integration** | `user.repository.test.ts` | Репозиторий + реальная SQLite in-memory |
| **E2E** | `api.test.ts` | Полный HTTP-цикл: запрос → все слои → ответ |

Чистая архитектура делает тестирование **естественным**: домен тестируется с тривиальными моками (обычные объекты, реализующие интерфейсы), без мок-фреймворков, без реальной БД.

---

## 📚 Документация

| Файл | Содержание |
|------|------------|
| [queries.md](queries.md) | Примеры cURL-запросов для всех эндпоинтов и сценариев |
| Коммиты | Каждый коммит содержит детальное описание и пояснения |
